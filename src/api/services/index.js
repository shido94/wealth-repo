const userService = require('./user.service');
const authService = require('./auth.service');
const tokenService = require('./token.service');

module.exports = { userService, authService, tokenService };
